logging:
  path: /usr/log/xzs/

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:host.docker.internal}:${DB_PORT:3306}/${DB_NAME:xzs}?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: ${DB_USER:xzs}
    password: ${DB_PASS:xzs123}
    driver-class-name: com.mysql.cj.jdbc.Driver
